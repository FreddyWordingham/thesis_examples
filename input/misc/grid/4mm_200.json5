{   // Sampling grid settings.
    boundary: {                             // Boundary limits.
        maxs: [ 2.0e-3,  2.0e-3,  2.0e-3],  // Maximum bound.
        mins: [-2.0e-3, -2.0e-3, -2.0e-3],  // Minimum bound.
    },
    res: [200, 200, 200],                      // Sampling resolution in [x,y,z].
}
